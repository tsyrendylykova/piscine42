#include <sys/types.h>
#include <sys/uio.h>
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

int		main(void)
{
	char	c;
	char	*buffer = NULL;
	unsigned int len;
	int		read;
	//t_list	*list;

	//list = NULL;
	float f;
	f = 5 / 3;
	printf("%f\n", f);
	//while (read(0, &c, 1))
	//{
	//	ft_list_push_back(&list, c);
	//}
	//print_list(list);
	return (0);
}
